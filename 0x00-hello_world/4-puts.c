#include <stadio.h>
/**
 * main - A program that prints a line iwth puts function
 * Return: o (success)
 */
int main(void)
{
puts("\"programming is like building a multilingual puzzle);
reture (o);
}
